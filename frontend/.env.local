# URL interna que usan los Server Components para fetch directo al backend
# (no pasa por el rewrite de Next.js)
BACKEND_URL=http://localhost:3000

# URL pública para mutaciones client-side (formularios "use client")
NEXT_PUBLIC_API_URL=http://localhost:3000
